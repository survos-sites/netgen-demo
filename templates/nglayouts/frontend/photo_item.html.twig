<div class="text-center recipe-container p-3">
    <div class="p-3 entity-img">
        <img src="{{ asset(item.object.imageUrl) }}" width="auto" height="115" alt="{{ item.object.title }} image">
    </div>
    <h3 class="mt-3">{{ item.object.title }}</h3>
</div>
